# Local scratch / working folders
_scratch/

# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store

# Claude Code local settings (per-machine)
.claude/settings.local.json

# Config files the setup instructions tell users to create themselves —
# never commit a real one, it contradicts the instructions and can go stale
.mcp.json
.agents/

# Image binaries — keep sample assets small or out of git
*.afphoto
*.afmacro
*.tif
*.tiff
*.psd
*.raw
*.cr2
*.nef
*.arw
*.dng
